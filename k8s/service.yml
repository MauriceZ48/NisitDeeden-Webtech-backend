apiVersion: v1
kind: Service
metadata:
  name: nisit-deeden-nginx
spec:
  type: NodePort
  selector:
    app: nisit-deeden-laravel
  ports:
    - protocol: TCP
      port: 80
      nodePort: 30080
      targetPort: 9000
